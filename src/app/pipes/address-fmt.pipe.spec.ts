import { AddressFmtPipe } from './address-fmt.pipe';

describe('AddressFmtPipe', () => {
  it('create an instance', () => {
    const pipe = new AddressFmtPipe();
    expect(pipe).toBeTruthy();
  });
});
